---
import { Icon } from "astro-icon/components"

interface Props {
	href: string
	// class?: string
	icon: string
	label: string
}

const { href, icon, label } = Astro.props
---

<a
	href={href}
	class="button button--primary"
>
	<Icon
		name={icon}
		aria-hidden="true"
	/>{label}
</a>
